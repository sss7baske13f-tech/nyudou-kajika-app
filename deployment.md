# 深海プロフィール帳 - Deployment Guide (Netlify)

このアプリは静的な HTML/CSS/JS で構成されているため、Netlify を使って数秒で公開できます。

## 修正・追加された機能
- **筆跡診断 (Handwriting Diagnosis)**: 3つの質問に答えることで、最適な筆跡を自動決定します。
- **50種類のフォント**: 日本語・英語合わせて50種類の豊かな筆跡バリエーション。
- **タブ切り替え & プロフ交換**: すべてのボタンとタブ機能が正常に動作するように修複済み。

## Netlify への公開手順 (ドラッグ＆ドロップ)

1. **[Netlify にログイン](https://app.netlify.com/)** します。
2. **「Sites」** タブを開き、一番下にある **「Deploy a new site without connecting to Git」** というエリア（点線の枠）を探します。
3. エクスプローラーでこの `ExchangeDiary` フォルダを開き、**フォルダごと** その枠の中にドラッグ＆ドロップしてください。
4. 数秒でアップロードが完了し、公開URLが発行されます！

## 公開するファイル構成
デプロイ時には以下のファイルが含まれていることを確認してください：
- `index.html` (メイン画面)
- `style.css` / `heisei_style.css` (デザイン)
- `app.js` (プログラム本体)
- `data.js` (データベース・質問データ)
- `assets/` (魚のキャラクター画像など)

## 注意事項
- このアプリはブラウザの `localStorage` を使用してデータを保存します。別のブラウザやシークレットモードで開くとデータは引き継がれません。
- QRコード交換機能は、生成されたコードを友達に見せる（または画像を共有する）ことで動作します。
